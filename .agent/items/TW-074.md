id: TW-074
title: Cite §31 at the five ratified findings thresholds and pin them
status: needs-approval
tags: —
order: 850

why: `reef-chemistry.md` §31, added 2026-08-16 (Stage 5). Five of
`buildFindings`' thresholds survive the deletions in Part 3 of
`.agent/gap-report.md` and are **ratified at their current values**. They were
never wrong; they had no entry in canon, which is a different fault and the one
Stage 5 exists to close.
the five, with their sites in `src/lib/findings.js`:
  - implausible alkalinity consumption, `> 2 dKH/day` (`:413`)
  - implausible calcium consumption, `> 30 ppm/day` (`:426`)
  - ionic ratio off, more than **15%** either side of §20's 7.15 coupling
    (`:298`)
  - pH high, `> 8.45` (`:575`)
  - CO2 signature, pH `< 7.9` with alkalinity in range (`:569`)
the work is small and is deliberately **not** a retune: cite §31 where each is
defined, and pin each with a test so a later refactor cannot move one quietly.
Changing a value is out of scope for this item and would need its own decision.
what this item does not cover, so nobody reads §31 as ratifying more than it
does: every other unnamed figure in that file belongs to a finding Part 3
deletes. A number attached to a deleted finding gets no canon entry and no test
here — deleting them is the finding work, not this.
in plain terms: five of the app's checks keep exactly the numbers they already
had — two that catch an impossible consumption figure, one on the
calcium-to-alkalinity relationship, and two on pH. They were right, they were
just never written down. This is the job of writing them down in the code and
locking them with a test.
spec: docs/spec/reef-chemistry.md §31, §20 (the 7.15 coupling), §16;
`.agent/gap-report.md` Part 1.5 and Part 3
repro: `src/lib/findings.js` — five literals, no citation, no test.
tests it needs: one per threshold, each asserting the boundary and citing §31.
owner: implementer, once [approved]
