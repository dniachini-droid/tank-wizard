id: TW-068
title: Delete the health score, `ScoreBreakdown` and both score colour bands
status: needs-approval
tags: —
order: 790

why: `wizard-states.md` §25.1, decided 2026-08-16 (Stage 5). A 0-100 number on
the front of the app computed from **nineteen constants** in
`src/lib/narrative-engine.js`, with **no canon entry of any kind** — not a band,
not a verdict, not a notice, not a message. It has already been caught showing
42 while its own working panel summed to 71.
the reason, which is the app's own first wording rule (§23.1): state it, then
show the basis. A single number claiming to summarise a tank's health is the
least checkable thing in the app — there is no basis to show, only nineteen
weights nobody has seen.
what goes: the score itself, `ScoreBreakdown`, the score colour bands
(`TodayPanel.jsx:623`) and the headline score bands (`narrative-engine.js:848-851`),
and with them the nineteen figures the gap report inventoried at Part 1.5 —
stability points by grade, the out-of-band position points, the 0.4/0.6 blend,
the mean-vs-worst 0.6/0.4, the two ammonia caps, the evidence caps, and
`safetyCapFor`'s 45-to-15 range.
what replaces it: **the collapsed tank summary headline, and G-27 is answered.**
Decided 16 August, later the same day this item was filed: three slots — the
worst thing now, anything else notable, anything in flight — each dropped when
empty, generated from the same verdicts the tiles render.
`wizard-states.md` §25.1, and the implementation is **TW-076**. **The candidate
recorded here was not adopted:** *"3 of 6 in range, 2 need attention"* counts the
quiet as well as the loud, and what may be counted as in range and holding is
G-29, still open.
sequencing, and the reason this item is not simply a delete: the score is the
only tank-level view on the front screen today. **Deleting it before TW-076
lands leaves the collapsed summary with whatever `buildHeadline` produces**, so
the two ship together or the replacement ships first. An implementer must not
fill the hole with an invented replacement — the wording is §25.1's and nowhere
else.
check on the way out: `narrative-engine.js` also owns `buildHeadline` and
`buildBriefing`, which are **not** deleted here. Delete the score's constants
without taking the headline's with them.
in plain terms: the number out of 100 on your front screen goes. Nothing in your
spec ever said what it meant, it was built from nineteen weightings nobody has
seen, and it once said 42 while its own breakdown added up to 71. What replaces
it is a line you can actually check by looking at the tiles underneath it — the
worst thing first, then anything else worth mentioning, then anything already
under way.
spec: docs/spec/wizard-states.md §25.1 (the deletion and the candidate), §23.1,
§20 and §25.1's collapsed headline rule; `.agent/gap-report.md` Part 1.5,
G-27 (answered) and G-29 (open)
repro: `.agent/gap-report.md` Part 1.5 — 19 numbers, one number on screen, zero
canon.
tests it needs: mostly deletions. One test that no surface renders a
tank-level score, so it cannot come back quietly.
owner: implementer, once [approved] — the replacement is TW-076 and is no longer
blocked; the deletion never was
