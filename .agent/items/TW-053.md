id: TW-053
title: Eleven `.agent/backlog.md` paths in docs/spec and docs/journeys point at a deleted file
status: needs-approval
tags: —
order: 540

why: TW-042 replaced `.agent/backlog.md` with one file per item under
`.agent/items/`. Every reference in the agent-facing tree was repointed in the
same change. Eleven were not, all of them in human-authored canon, which
AGENTS.md rule 1 forbids an agent to edit:
  - `docs/spec/reef-chemistry.md` :862 (TW-002), :875 (TW-049), :1377 (TW-029),
    :1440 (TW-028), :1880 (TW-050), :1891 (TW-047), :2049 (TW-048)
  - `docs/spec/wizard-states.md` :595 (TW-031), :832 (TW-031), :1103 (no id —
    "Filed in `.agent/backlog.md`")
  - `docs/journeys/journey-5-phosphate-nitrate.md` :6 (TW-029)
Every one of them names an item that still exists and still resolves; only the
path is stale. None of the ids were renumbered, so no reference points at the
wrong item — a reader following one lands on a file that is not there rather
than on the wrong thing.
in plain terms: ten pointers in Dan's own spec documents, and one in a journey,
say "look in the backlog file" and that file has moved to a folder. Nothing is
lost and nothing is wrong; the signposts need repainting, and only Dan repaints
these ones.
spec: none — the change is *to* spec prose, not governed by it. AGENTS.md rule 1
is why this is filed rather than done.
repro: `grep -n 'backlog\.md' docs/spec/*.md docs/journeys/*.md` — 11 hits, all
of a path that no longer exists.
suggested fix: `.agent/backlog.md` → `.agent/items/`, or
`.agent/backlog.md TW-0NN` → `.agent/items/TW-0NN.md`, in place. Mechanical, no
meaning changes. wizard-states.md:1103 has no id, so "Filed in `.agent/items/`"
is the whole edit there.
owner: Dan, or an agent with a one-off authorisation naming these eleven lines
