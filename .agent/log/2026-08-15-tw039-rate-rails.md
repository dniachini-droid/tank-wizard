# Run log — TW-039 (rate-rails.test.js stale canon)

run: 2026-08-15-tw039-rate-rails
item: TW-039 [approved]
branch: claude/four-backlog-items-o18y6d-tw039

## What happened

Both stale blocks in `src/test/spec/dosing/rate-rails.test.js` updated to
current canon (reef-chemistry.md §3, decided 14 Aug). No app code touched —
the code was already correct; the test file was the leftover.

1. Rail-default expectations: calcium 25 → **20 ppm/24h**, magnesium 100 →
   **25 ppm/24h**. Citation corrected from "§6, lines 149-166" (a range that
   no longer exists) to §3, lines 146-166 (table at :156-161).
2. The `describe('§6 — a user may tighten a rail; the app must honour it')`
   block **inverted, not deleted** (AGENTS.md rule 4): it now asserts a
   user-supplied `maxDailyRiseDKH` changes nothing — identical output with
   and without the field, clamp is the fixed §3 rail. Retitled to §3; the
   ":89-91 spec requires the tighter ceiling to win" comment dropped as it
   stated the opposite of canon.

## Evidence

- Before: `npx vitest run src/test/spec/dosing/rate-rails.test.js` →
  3 failed / 3 passed (calcium 25, magnesium 100, tighter-rail honoured).
- After: same command → 6 passed.
- `npm run verify` → ALL BLOCKING CHECKS PASSED (deadcode/csscheck advisory
  failures pre-existing, unrelated).

## In plain terms

A leftover checklist still said magnesium may rise 100 ppm a day and calcium
25, and that you could ask the app for a gentler limit. The decided ceilings
are 25 and 20, and there is no user dial — so the checklist now checks the
real rules, including that asking for a gentler limit changes nothing.
